# WB Real Stock Bot

Telegram-бот для двух кабинетов Wildberries, который формирует Excel с раздельными показателями:

- **Остаток WB (физический FBO/FBW)** — новый официальный аналитический отчёт WB.
- **FBS** — сумма остатков по всем складам продавца конкретного кабинета.
- **Реально доступно FBO** — онлайн-остаток покупательской витрины WB, только `dtype=4`.
- **Скрыто / недоступно FBO** = физический FBO − реальный FBO.
- **Общий физический остаток** = физический FBO + FBS.

## Почему FBS не вычитается из реального FBO

Витрина WB сама разделяет остатки по типу продажи:
- `dtype=4` — FBO/FBW;
- `dtype=1` — FBS.

Бот суммирует для "реального FBO" только `dtype=4`, поэтому FBS берётся независимо из API склада продавца.

## Контрольные артикула

По умолчанию добавлена кнопка **«🧪 Проверить контрольные артикула»**:

- `16358648` → ожидается FBO `0`
- `219205510` → ожидается FBO `0`
- `206008365` → ожидается FBO `4`

Если цифры отличаются, проверьте `WB_DEST`: видимость/доступность товара в витрине зависит от региона доставки.

## Подготовка токенов WB

Для каждого из двух кабинетов нужен токен с доступом как минимум к категориям:

1. **Контент** — получить карточки, артикулы, chrtID и баркоды.
2. **Маркетплейс** — получить FBS-склады и их остатки.
3. **Аналитика** — новый отчёт текущих остатков на складах WB.

Токены разных кабинетов хранятся отдельно.

## Установка

Python 3.11+.

```bash
python -m venv .venv
```

Windows:

```bash
.venv\Scripts\activate
```

Linux/macOS:

```bash
source .venv/bin/activate
```

Установите зависимости:

```bash
pip install -r requirements.txt
```

Скопируйте `.env.example` в `.env` и заполните:

```env
TELEGRAM_BOT_TOKEN=123456:ABC...

WB_CABINET_1_NAME=Первый кабинет
WB_CABINET_1_TOKEN=...

WB_CABINET_2_NAME=Второй кабинет
WB_CABINET_2_TOKEN=...

WB_DEST=-1257786
```

Запуск:

```bash
python bot.py
```

## Кнопки Telegram

- **📦 Первый кабинет** — Excel только по первому кабинету.
- **📦 Второй кабинет** — Excel только по второму кабинету.
- **📊 Оба кабинета** — один `.xlsx`, два отдельных листа.
- **🧪 Проверить контрольные артикула** — сверка покупательского FBO.

## Колонки Excel

1. Баркод
2. Артикул продавца
3. Артикул WB
4. Название
5. Остаток WB (физический FBO)
6. FBS (склады продавца)
7. Реально доступно FBO
8. Скрыто / недоступно FBO
9. Общий физический остаток
10. Статус

## Важное замечание про «реально доступно»

Публичная карточка WB — не официальный Seller API и может меняться без предупреждения. Поэтому код вынесен в отдельный метод `get_storefront_real_fbo()` в `wb_client.py`. Если WB изменит v4 endpoint или схему ответа, остальные части бота менять не потребуется.

`WB_DEST` важен: покупательская доступность может различаться в зависимости от региона/адреса доставки.

## Безопасность

- Не отправляйте WB API-токены в Telegram.
- Не коммитьте `.env` в Git.
- Можно ограничить доступ к боту через `ALLOWED_TELEGRAM_USER_IDS`.

Пример:

```env
ALLOWED_TELEGRAM_USER_IDS=123456789,987654321
```
